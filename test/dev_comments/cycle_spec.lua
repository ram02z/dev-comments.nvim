local cycle = require("dev_comments.cycle")
local mock = require("luassert.mock")

describe("cycle", function()
  it("moves to next dev comment", function() end)
  it("moves to next prev comment", function() end)
end)
